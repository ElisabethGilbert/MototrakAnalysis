function plot_mean_peak(folder)

D = dir(['C:\MotoTrak Files\170\*.mat']);



numfich = size(D,1);

for i = 1:numfich
   
    load ('170_MotoTrak_Knob_Results_20220527_112911.mat')

    
    
   % hit_thresh = data(:,3)
    
  %  figure
  % plot(
    
  
   % 
end

%file = D.name
%data = load(strcat(D,D.name(i)));